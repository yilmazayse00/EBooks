<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ebooks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EBooks">
        <activity android:name=".dbScreens.SignupScreen" android:parentActivityName=".dbScreens.LoginScreen"></activity>
        <activity android:name=".dbScreens.LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme"></activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".BookDetailsScreen"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>